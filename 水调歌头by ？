-- SDGT | 水调歌头 (WindUI 完整整合版)
-- 所有功能开关均带通知提醒

loadstring(game:HttpGet('https://raw.githubusercontent.com/Footagesus/WindUI/refs/heads/main/main_example.lua'))()

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local Workspace = game:GetService("Workspace")
local Lighting = game:GetService("Lighting")
local UserInputService = game:GetService("UserInputService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local VirtualInputManager = game:GetService("VirtualInputManager")
local HttpService = game:GetService("HttpService")
local TeleportService = game:GetService("TeleportService")
local LP = Players.LocalPlayer

-- ==================== 全局配置变量 ====================
-- 杀戮光环配置
KILLAURA_ON = false
AUTO_FACE_ON = false
JUMP_SWING_ON = false
AUTO_EQUIP_ON = false
lastAttackTime = 0
currentTarget = nil
currentRemote = nil
currentConfig = nil
LOOP_SPEED_VAL = 16
FULL_BRIGHT_ON = false

WEAPON_DB = {
    ["Stake"] = {range = 13, delay = 0.12, swing = "Over", mult = 12, path = "backpack"},
    ["Hand Axe"] = {range = 14, delay = 0.10, swing = "Over", mult = 12, path = "workspace"},
    ["Boarding Axe"] = {range = 15, delay = 0.10, swing = "Over", mult = 15, path = "backpack"},
    ["Heavy Sabre"] = {range = 15, delay = 0.10, swing = "Thrust", mult = 18, path = "workspace"},
    ["Sabre"] = {range = 15, delay = 0.10, swing = "Thrust", mult = 18, path = "workspace"},
    ["Axe"] = {range = 14, delay = 0.10, swing = "Side", mult = 12, path = "workspace"},
    ["Pickaxe"] = {range = 14, delay = 0.12, swing = "Over", mult = 12, path = "workspace"},
    ["Spontoon"] = {range = 25, delay = 0.10, swing = "Thrust", mult = 12, path = "backpack"},
    ["Officer's Sabre"] = {range = 15, delay = 0.10, swing = "Thrust", mult = 15, path = "backpack"}
}

ELBOW_CONFIGS = {
    ["工兵斧"] = {weapon = "Axe", range = 14, delay = 0.10},
    ["卡宾枪"] = {weapon = "Carbine", range = 14, delay = 0.10}
}

-- 透视相关
activeStates = {}
espHighlights = {}
ALERT_RANGE = 20

-- 飞行相关
nowe = false
speeds = 1
tpwalking = false

-- 其他功能标志
noRecoilEnabled = false
isAntiSelfDamage = false
isAntiKnockback = false
isAntiRagdoll = false
AutoBellActive = false
AutoBucketActive = false
AutoRepairBridge = false
AutoGetLogs = false
AutoPlaceLogs = false
AutoRequestHealEnabled = false
AutoRepairEnabled = false
AutoBlessEnabled = false
AimlockBomberEnabled = false
AutoJumpEnabled = false
KeepBackpackActive = false
NoSlowActive = false
SelfRescueActive = false
NoFallDamageEnabled = false
MusicDiamondsEnabled = false
HitboxExpanderEnabled = false
AutoShootBomber = false
AutoShootCavalry = false
AutoShootRunner = false
AutoShootElectrocutioner = false
AutoShootIgniter = false
AutoReloadEnabled = false
-- 玩家透视
PlayerChamsEnabled = false
PlayerBillboardEnabled = false
NotifyLowHealth = false
NotifyGrabbedPinned = false
ShowInfected = false
-- 职业功能
HealThreshold = 25
BlessThreshold = 50

-- ==================== 辅助函数 ====================
local function Notify(title, content, duration)
    pcall(function()
        WindUI:Notify({Title = title, Content = content, Duration = duration or 2, Icon = "bell"})
    end)
end

-- 危险核心判断（来自第一个文件）
local function isDangerousCore(obj)
    if not obj or not obj:IsA("Model") then return true end
    local current = obj
    while current and current ~= workspace do
        if current.Name == "m_Zombie" or current:FindFirstChild("Barrel", true) or current.Parent == workspace:FindFirstChild("Camera") then return true end
        current = current.Parent
    end
    local cam = workspace:FindFirstChild("Camera")
    if cam then
        for _, b in ipairs(cam:GetChildren()) do
            if b.Name == "m_Zombie" and b:FindFirstChild("Barrel", true) then
                local bRoot = b:FindFirstChild("HumanoidRootPart") or b:FindFirstChild("Torso")
                local objPos = (obj:FindFirstChild("HumanoidRootPart") or obj:FindFirstChild("Torso") or {Position = Vector3.new(9e9,9e9,9e9)}).Position
                if bRoot and (bRoot.Position - objPos).Magnitude < 4 then return true end
            end
        end
    end
    return false
end

-- 扫描目标（杀戮光环）
local function scanTarget()
    if not KILLAURA_ON then currentTarget = nil return end
    local hrp = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    local closest, minDist = nil, 25
    local folders = {workspace:FindFirstChild("Zombies"), workspace:FindFirstChild("Camera")}
    for _, f in pairs(folders) do if f then for _, z in ipairs(f:GetChildren()) do
        if z:IsA("Model") and not isDangerousCore(z) then
            local zr = z:FindFirstChild("HumanoidRootPart") or z:FindFirstChild("Torso")
            local hum = z:FindFirstChildOfClass("Humanoid")
            if zr and hum and hum.Health > 0 then 
                local d = (hrp.Position - zr.Position).Magnitude 
                if d < minDist then closest, minDist = z, d end 
            end
        end
    end end end
    currentTarget = closest
end

-- 自动装备武器
local function autoEquipWeapon()
    if not AUTO_EQUIP_ON then return end
    local char = LP.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if not hrp then return end
    if char:FindFirstChildOfClass("Tool") then return end
    local zombieNearby = false
    local zombies = workspace:FindFirstChild("Zombies")
    if zombies then
        for _, z in ipairs(zombies:GetChildren()) do
            if z:IsA("Model") and not isDangerousCore(z) then
                local zRoot = z:FindFirstChild("HumanoidRootPart") or z:FindFirstChild("Torso")
                local hum = z:FindFirstChildOfClass("Humanoid")
                if zRoot and hum and hum.Health > 0 then
                    local dist = (hrp.Position - zRoot.Position).Magnitude
                    if dist <= 30 then zombieNearby = true; break end
                end
            end
        end
    end
    if zombieNearby then
        for name, _ in pairs(WEAPON_DB) do
            local tool = LP.Backpack:FindFirstChild(name)
            if tool then
                local hum = char:FindFirstChildOfClass("Humanoid")
                if hum then hum:EquipTool(tool); break end
            end
        end
    end
end

-- 肘击光环
local lastElbowTimes = {}
local function getElbowRemote(weaponName)
    local tool = LP.Backpack:FindFirstChild(weaponName) or (LP.Character and LP.Character:FindFirstChild(weaponName))
    return tool and tool:FindFirstChild("RemoteEvent")
end
local function findElbowTarget(range)
    local char = LP.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    local zombies = workspace:FindFirstChild("Zombies")
    if not hrp or not zombies then return nil end
    local closest, dist = nil, range
    for _, z in pairs(zombies:GetChildren()) do
        if z:IsA("Model") and not z:FindFirstChild("Barrel", true) and z.Name ~= "m_Zombie" then
            local zHrp = z:FindFirstChild("HumanoidRootPart") or z:FindFirstChild("Torso")
            local hum = z:FindFirstChildOfClass("Humanoid")
            if zHrp and hum and hum.Health > 0 then
                local d = (hrp.Position - zHrp.Position).Magnitude
                if d < dist then closest = z; dist = d end
            end
        end
    end
    return closest
end
local function attackElbow(target, weaponType, lastTime, delay)
    local now = tick()
    if now - lastTime < delay then return lastTime end
    local remote = getElbowRemote(weaponType == "工兵斧" and "Axe" or "Carbine")
    local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")
    local tRoot = target:FindFirstChild("HumanoidRootPart") or target:FindFirstChild("Head")
    if not remote or not root or not tRoot then return lastTime end
    pcall(function()
        if weaponType == "工兵斧" then
            remote:FireServer("Equip", root); remote:FireServer("BraceBlock"); remote:FireServer("FeedbackStun", target, tRoot.Position); remote:FireServer("StopBraceBlock")
        else
            remote:FireServer("Shove"); remote:FireServer("FeedbackStun", target, tRoot.Position)
        end
    end)
    return now
end

-- ESP创建
local function createESP(model, color, label)
    if espHighlights[model] then return end
    local torso = model:FindFirstChild("Torso") or model:FindFirstChild("HumanoidRootPart")
    if not torso then return end
    local hl = Instance.new("Highlight", model); hl.FillColor, hl.OutlineColor = color, Color3.new(1,1,1); hl.FillTransparency = 0.4; hl.DepthMode = "AlwaysOnTop"
    local bgui = Instance.new("BillboardGui", torso); bgui.Size, bgui.AlwaysOnTop, bgui.StudsOffset = UDim2.new(0,80,0,30), true, Vector3.new(0,3,0)
    local tl = Instance.new("TextLabel", bgui); tl.BackgroundTransparency, tl.Size, tl.Text = 1, UDim2.new(1,0,1,0), label; tl.TextColor3, tl.TextStrokeTransparency, tl.TextSize, tl.Font = color, 0, 14, "SourceSansBold"
    espHighlights[model] = {hl = hl, gui = bgui}
end

-- 获取物品（第二个文件）
local function purchaseItem(itemName)
    local purchaseEvent = ReplicatedStorage:WaitForChild("Events"):WaitForChild("Customize"):WaitForChild("PurchaseEvent")
    purchaseEvent:FireServer(itemName)
end

-- ==================== WindUI 界面构建 ====================
local Window = WindUI:CreateWindow({
    Title = "SDGT | 水调歌头",
    Icon = "sword",
    Theme = "Dark",
    Size = UDim2.new(0, 550, 0, 450),
})

-- 功能选项卡
local Tab_Main = Window:AddTab("功能", "home")
local Tab_KillAura = Window:AddTab("杀戮光环", "sword")
local Tab_ESP = Window:AddTab("透视", "eye")
local Tab_Class = Window:AddTab("职业功能", "users")
local Tab_Player = Window:AddTab("玩家功能", "user")

-- ==================== 功能选项卡内容 ====================
-- 主要分区
local MainGroup = Tab_Main:AddLeftGroupbox("主要")
MainGroup:AddToggle({
    Title = "杀戮光环 (核心)",
    Icon = "sword",
    Default = false,
    Callback = function(state)
        KILLAURA_ON = state
        Notify("杀戮光环", state and "已开启" or "已关闭", 1)
        if state then
            task.spawn(function()
                while KILLAURA_ON and task.wait(0.1) do
                    scanTarget()
                    autoEquipWeapon()
                    local char = LP.Character
                    if char then
                        local tool = char:FindFirstChildOfClass("Tool")
                        if tool and WEAPON_DB[tool.Name] then
                            currentRemote, currentConfig = tool:FindFirstChild("RemoteEvent"), WEAPON_DB[tool.Name]
                        end
                    end
                    if KILLAURA_ON and currentTarget and currentRemote and currentConfig then
                        local now = tick()
                        if now - lastAttackTime >= currentConfig.delay then
                            local head = currentTarget:FindFirstChild("Head") or currentTarget:FindFirstChild("HumanoidRootPart")
                            local hrp = char and char:FindFirstChild("HumanoidRootPart")
                            if head and hrp then
                                local dir = (head.Position - hrp.Position).Unit
                                pcall(function()
                                    currentRemote:FireServer("Swing", currentConfig.swing)
                                    currentRemote:FireServer("HitZombie", currentTarget, head.Position, true, dir * 15, "Head", dir)
                                end)
                                lastAttackTime = now
                            end
                        end
                    end
                end
            end)
        end
    end
})
MainGroup:AddToggle({
    Title = "自动朝向",
    Icon = "target",
    Default = false,
    Callback = function(state)
        AUTO_FACE_ON = state
        Notify("自动朝向", state and "已开启" or "已关闭", 1)
    end
})
MainGroup:AddToggle({
    Title = "挥刀跳",
    Icon = "arrow-up",
    Default = false,
    Callback = function(state)
        JUMP_SWING_ON = state
        Notify("挥刀跳", state and "已开启" or "已关闭", 1)
        if state then
            -- 挂钩子实现挥刀跳
            local oldNamecall = hookmetamethod(game, "__namecall", function(self, ...)
                local method = getnamecallmethod()
                local args = {...}
                if method == "FireServer" and tostring(self) == "RemoteEvent" then
                    if args[1] == "Swing" or args[1] == "Thrust" then
                        task.defer(function()
                            local char = LP.Character
                            local hum = char and char:FindFirstChildOfClass("Humanoid")
                            if hum and hum.FloorMaterial ~= Enum.Material.Air then hum.Jump = true end
                        end)
                    end
                end
                return oldNamecall(self, ...)
            end)
        else
            -- 恢复原钩子（简化处理，实际可保留但无影响）
        end
    end
})
MainGroup:AddToggle({
    Title = "自动装备武器",
    Icon = "box",
    Default = false,
    Callback = function(state)
        AUTO_EQUIP_ON = state
        Notify("自动装备", state and "已开启" or "已关闭", 1)
    end
})
MainGroup:AddToggle({
    Title = "全图高亮",
    Icon = "sun",
    Default = false,
    Callback = function(state)
        FULL_BRIGHT_ON = state
        if state then
            Lighting.Ambient = Color3.new(1,1,1)
            Lighting.OutdoorAmbient = Color3.new(1,1,1)
            Lighting.Brightness = 2
            Lighting.ClockTime = 14
        else
            Lighting.Ambient = Color3.new(0,0,0)
            Lighting.OutdoorAmbient = Color3.new(0,0,0)
            Lighting.Brightness = 1
            Lighting.ClockTime = 12
        end
        Notify("全图高亮", state and "已开启" or "已关闭", 1)
    end
})
MainGroup:AddSlider({
    Title = "移动速度",
    Icon = "gauge",
    Default = 16,
    Min = 16,
    Max = 45,
    Callback = function(val)
        LOOP_SPEED_VAL = val
        local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.WalkSpeed = val end
        Notify("移动速度", "已设置为 " .. val, 1)
    end
})

-- 自动飞行分区
local FlightGroup = Tab_Main:AddRightGroupbox("自动飞行")
FlightGroup:AddToggle({
    Title = "飞行绕过",
    Icon = "plane",
    Default = false,
    Callback = function(state)
        nowe = state
        local chr = LP.Character
        local hum = chr and chr:FindFirstChildWhichIsA("Humanoid")
        local humRootPart = chr and chr:FindFirstChild("HumanoidRootPart")
        if not hum or not humRootPart then return end
        if nowe then
            humRootPart.CanCollide = false
            tpwalking = true
            for i = 1, speeds do
                task.spawn(function()
                    while tpwalking and task.wait() do
                        local chr = LP.Character
                        local hum = chr and chr:FindFirstChildWhichIsA("Humanoid")
                        if chr and hum and hum.Parent then
                            if hum.MoveDirection.Magnitude > 0 then
                                chr:TranslateBy(hum.MoveDirection)
                            end
                        end
                    end
                end)
            end
            hum:SetStateEnabled(Enum.HumanoidStateType.Climbing, false)
            hum:SetStateEnabled(Enum.HumanoidStateType.FallingDown, false)
            hum:SetStateEnabled(Enum.HumanoidStateType.Flying, false)
            hum:SetStateEnabled(Enum.HumanoidStateType.Freefall, false)
            hum:SetStateEnabled(Enum.HumanoidStateType.GettingUp, false)
            hum:SetStateEnabled(Enum.HumanoidStateType.Jumping, false)
            hum:SetStateEnabled(Enum.HumanoidStateType.Landed, false)
            hum:SetStateEnabled(Enum.HumanoidStateType.Physics, false)
            hum:SetStateEnabled(Enum.HumanoidStateType.PlatformStanding, false)
            hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false)
            hum:SetStateEnabled(Enum.HumanoidStateType.Running, false)
            hum:SetStateEnabled(Enum.HumanoidStateType.RunningNoPhysics, false)
            hum:SetStateEnabled(Enum.HumanoidStateType.Seated, false)
            hum:SetStateEnabled(Enum.HumanoidStateType.StrafingNoPhysics, false)
            hum:SetStateEnabled(Enum.HumanoidStateType.Swimming, false)
            hum:ChangeState(Enum.HumanoidStateType.Swimming)
            if chr:FindFirstChild("Animate") then chr.Animate.Disabled = true end
            for _, v in next, hum:GetPlayingAnimationTracks() do v:AdjustSpeed(0) end
            local rootPart = hum.RigType == Enum.HumanoidRigType.R6 and chr.Torso or chr.UpperTorso
            local bg = Instance.new("BodyGyro", rootPart)
            bg.P = 9e4
            bg.maxTorque = Vector3.new(9e9, 9e9, 9e9)
            bg.cframe = rootPart.CFrame
            local bv = Instance.new("BodyVelocity", rootPart)
            bv.velocity = Vector3.new(0, 0.1, 0)
            bv.maxForce = Vector3.new(9e9, 9e9, 9e9)
            hum.PlatformStand = true
            local ctrl = { f = 0, b = 0, l = 0, r = 0 }
            local maxspeed = 50
            local flySpeed = 0
            task.spawn(function()
                while nowe and hum.Health > 0 do
                    task.wait()
                    ctrl.f = UserInputService:IsKeyDown(Enum.KeyCode.W) and 1 or 0
                    ctrl.b = UserInputService:IsKeyDown(Enum.KeyCode.S) and 1 or 0
                    ctrl.l = UserInputService:IsKeyDown(Enum.KeyCode.A) and 1 or 0
                    ctrl.r = UserInputService:IsKeyDown(Enum.KeyCode.D) and 1 or 0
                    if ctrl.l + ctrl.r ~= 0 or ctrl.f + ctrl.b ~= 0 then
                        flySpeed = flySpeed + 0.5 + (flySpeed / maxspeed)
                        flySpeed = math.clamp(flySpeed, 0, maxspeed)
                    else
                        flySpeed = math.clamp(flySpeed - 1, 0, maxspeed)
                    end
                    if flySpeed > 0 then
                        local dir = (Workspace.CurrentCamera.CoordinateFrame.lookVector * (ctrl.f + ctrl.b)) +
                            ((Workspace.CurrentCamera.CoordinateFrame * CFrame.new(ctrl.l + ctrl.r, (ctrl.f + ctrl.b) * 0.2, 0).p) -
                             Workspace.CurrentCamera.CoordinateFrame.p)
                        bv.velocity = dir * flySpeed
                        bg.cframe = Workspace.CurrentCamera.CoordinateFrame *
                            CFrame.Angles(-math.rad((ctrl.f + ctrl.b) * 50 * flySpeed / maxspeed), 0, 0)
                    else
                        bv.velocity = Vector3.new(0, 0.1, 0)
                    end
                end
                bg:Destroy(); bv:Destroy()
                if hum and hum.Parent then hum.PlatformStand = false end
                if chr and chr:FindFirstChild("Animate") then chr.Animate.Disabled = false end
            end)
        else
            humRootPart.CanCollide = true
            tpwalking = false
            hum:SetStateEnabled(Enum.HumanoidStateType.Climbing, true)
            hum:SetStateEnabled(Enum.HumanoidStateType.FallingDown, true)
            hum:SetStateEnabled(Enum.HumanoidStateType.Flying, true)
            hum:SetStateEnabled(Enum.HumanoidStateType.Freefall, true)
            hum:SetStateEnabled(Enum.HumanoidStateType.GettingUp, true)
            hum:SetStateEnabled(Enum.HumanoidStateType.Jumping, true)
            hum:SetStateEnabled(Enum.HumanoidStateType.Landed, true)
            hum:SetStateEnabled(Enum.HumanoidStateType.Physics, true)
            hum:SetStateEnabled(Enum.HumanoidStateType.PlatformStanding, true)
            hum:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, true)
            hum:SetStateEnabled(Enum.HumanoidStateType.Running, true)
            hum:SetStateEnabled(Enum.HumanoidStateType.RunningNoPhysics, true)
            hum:SetStateEnabled(Enum.HumanoidStateType.Seated, true)
            hum:SetStateEnabled(Enum.HumanoidStateType.StrafingNoPhysics, true)
            hum:SetStateEnabled(Enum.HumanoidStateType.Swimming, true)
            hum:ChangeState(Enum.HumanoidStateType.RunningNoPhysics)
            if chr:FindFirstChild("Animate") then chr.Animate.Disabled = false end
        end
        Notify("飞行绕过", state and "已开启" or "已关闭", 1)
    end
})
FlightGroup:AddSlider({
    Title = "飞行速度",
    Default = 1,
    Min = 1,
    Max = 10,
    Callback = function(val) speeds = val end
})

-- 枪械功能分区
local GunGroup = Tab_Main:AddLeftGroupbox("枪械功能")
GunGroup:AddToggle({
    Title = "自动装填",
    Icon = "refresh-cw",
    Default = false,
    Callback = function(state)
        AutoReloadEnabled = state
        Notify("自动装填", state and "已开启" or "已关闭", 1)
    end
})
GunGroup:AddToggle({
    Title = "自动射击炸弹僵尸",
    Default = false,
    Callback = function(state) AutoShootBomber = state end
})
GunGroup:AddToggle({
    Title = "自动射击胸甲骑兵",
    Default = false,
    Callback = function(state) AutoShootCavalry = state end
})
GunGroup:AddToggle({
    Title = "自动射击红眼",
    Default = false,
    Callback = function(state) AutoShootRunner = state end
})
GunGroup:AddToggle({
    Title = "自动射击斧头僵尸",
    Default = false,
    Callback = function(state) AutoShootElectrocutioner = state end
})
GunGroup:AddToggle({
    Title = "自动射击提灯人",
    Default = false,
    Callback = function(state) AutoShootIgniter = state end
})
-- 伴无敌按钮（直接执行碰飞.lua）
GunGroup:AddButton({
    Title = "伴无敌",
    Icon = "shield",
    Callback = function()
        Notify("伴无敌", "已激活，输入 ;kill 玩家名 来碰飞目标", 3)
        local Enabled = true
        local targetPlayer = nil
        local function findPlayer(text)
            text = text:lower()
            for _, plr in pairs(Players:GetPlayers()) do
                if plr.Name:lower():find(text) or plr.DisplayName:lower():find(text) then
                    return plr
                end
            end
        end
        LP.Chatted:Connect(function(msg)
            if not Enabled then return end
            if msg:sub(1,6):lower() == ";kill " then
                local name = msg:sub(7)
                local plr = findPlayer(name)
                if plr and plr.Character and plr.Character:FindFirstChild("Humanoid") then
                    targetPlayer = plr
                end
            end
        end)
        task.spawn(function()
            while Enabled and task.wait(0) do
                if not targetPlayer then continue end
                local char = targetPlayer.Character
                if not char or not char:FindFirstChild("Humanoid") or char.Humanoid.Health <= 0 then
                    targetPlayer = nil
                    continue
                end
                local myChar = LP.Character
                local myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
                local targetHRP = char:FindFirstChild("HumanoidRootPart")
                if myHRP and targetHRP then
                    local offset = targetHRP.Velocity.Magnitude < 0.1 and 0 or 7
                    local goal = targetHRP.CFrame * CFrame.new(0,0,-offset) * CFrame.Angles(0, math.rad(-3), 0)
                    myHRP.CFrame = myHRP.CFrame:Lerp(goal, 0.4)
                    myHRP.Velocity = Vector3.new(0,0,0)
                    myHRP.RotVelocity = Vector3.new(0,0,0)
                end
            end
        end)
        task.spawn(function()
            while Enabled and task.wait() do
                local hum = LP.Character and LP.Character:FindFirstChild("Humanoid")
                if hum then hum:Move(Vector3.one * 1e31) end
            end
        end)
    end
})

-- ==================== 杀戮光环选项卡 ====================
local KillGroup = Tab_KillAura:AddLeftGroupbox("光环")
KillGroup:AddToggle({
    Title = "刺刀杀戮光环",
    Default = false,
    Callback = function(state)
        Notify("刺刀杀戮光环", state and "已开启" or "已关闭", 1)
        if state then
            task.spawn(function()
                while state and task.wait(0.1) do
                    local char = LP.Character
                    if not char then continue end
                    local weapon = char:FindFirstChild("Musket") or LP.Backpack:FindFirstChild("Musket")
                    if not weapon then continue end
                    if weapon.Parent ~= char then weapon.Parent = char; task.wait(0.2) end
                    local remote = weapon:FindFirstChild("RemoteEvent")
                    if not remote then continue end
                    local zombies = workspace:FindFirstChild("Zombies")
                    if zombies then
                        for _, zombie in pairs(zombies:GetChildren()) do
                            if zombie:IsA("Model") and zombie:FindFirstChild("HumanoidRootPart") then
                                local dist = (char.HumanoidRootPart.Position - zombie.HumanoidRootPart.Position).Magnitude
                                if dist <= 15 then
                                    remote:FireServer("ThrustBayonet")
                                    task.wait(0.1)
                                    local hitPart = zombie:FindFirstChild("Head") or zombie:FindFirstChild("HumanoidRootPart")
                                    if hitPart then remote:FireServer("Bayonet_HitZombie", zombie, hitPart.Position, true) end
                                end
                            end
                        end
                    end
                end
            end)
        end
    end
})
KillGroup:AddToggle({
    Title = "肘击光环 (工兵斧)",
    Default = false,
    Callback = function(state)
        Notify("肘击光环", state and "已开启" or "已关闭", 1)
        if state then
            task.spawn(function()
                while state and task.wait(0.2) do
                    local char = LP.Character
                    if not char then continue end
                    local weapon = char:FindFirstChild("Axe") or LP.Backpack:FindFirstChild("Axe")
                    if not weapon then continue end
                    if weapon.Parent ~= char then weapon.Parent = char; task.wait(0.2) end
                    local remote = weapon:FindFirstChild("RemoteEvent")
                    if not remote then continue end
                    local zombies = workspace:FindFirstChild("Zombies")
                    if zombies then
                        for _, zombie in pairs(zombies:GetChildren()) do
                            if zombie:IsA("Model") and zombie:FindFirstChild("HumanoidRootPart") then
                                local dist = (char.HumanoidRootPart.Position - zombie.HumanoidRootPart.Position).Magnitude
                                if dist <= 15 then
                                    remote:FireServer("BraceBlock")
                                    task.wait(0.05)
                                    remote:FireServer("StopBraceBlock")
                                    remote:FireServer("FeedbackStun", zombie, zombie.HumanoidRootPart.CFrame.Position)
                                    task.wait(0.1)
                                end
                            end
                        end
                    end
                end
            end)
        end
    end
})
KillGroup:AddToggle({
    Title = "卡宾枪肘击",
    Default = false,
    Callback = function(state)
        Notify("卡宾枪肘击", state and "已开启" or "已关闭", 1)
        if state then
            task.spawn(function()
                while state and task.wait(0.5) do
                    local char = LP.Character
                    if not char then continue end
                    local carbine = char:FindFirstChild("Carbine") or LP.Backpack:FindFirstChild("Carbine")
                    if not carbine then continue end
                    local remote = carbine:FindFirstChild("RemoteEvent")
                    if not remote then continue end
                    local zombies = workspace:FindFirstChild("Zombies")
                    if zombies then
                        local targets = {}
                        for _, z in pairs(zombies:GetChildren()) do
                            if z:IsA("Model") and z:FindFirstChild("HumanoidRootPart") then
                                local dist = (char.HumanoidRootPart.Position - z.HumanoidRootPart.Position).Magnitude
                                if dist <= 15 then table.insert(targets, z) end
                            end
                        end
                        table.sort(targets, function(a,b)
                            return (a.HumanoidRootPart.Position - char.HumanoidRootPart.Position).Magnitude <
                                   (b.HumanoidRootPart.Position - char.HumanoidRootPart.Position).Magnitude
                        end)
                        local max = math.min(5, #targets)
                        if max > 0 then
                            remote:FireServer("Shove")
                            task.wait(0.2)
                            for i=1, max do
                                remote:FireServer("FeedbackStun", targets[i], targets[i].HumanoidRootPart.CFrame.Position)
                                task.wait(0.1)
                            end
                        end
                    end
                end
            end)
        end
    end
})

-- ==================== 透视选项卡 ====================
local ESPGroup = Tab_ESP:AddLeftGroupbox("僵尸透视")
local espTypes = {"自爆", "二师兄", "红眼", "大师兄", "燃烧者"}
for _, name in ipairs(espTypes) do
    ESPGroup:AddToggle({
        Title = name .. "透视",
        Default = false,
        Callback = function(state)
            activeStates[name] = state
            Notify(name .. "透视", state and "已开启" or "已关闭", 1)
            if state then
                task.spawn(function()
                    while activeStates[name] and task.wait(0.25) do
                        local cam = workspace:FindFirstChild("Camera")
                        if cam then
                            for _, obj in ipairs(cam:GetChildren()) do
                                if obj:IsA("Model") and ((name == "自爆" and obj:FindFirstChild("Torch", true)) or
                                    (name == "二师兄" and obj.Name == "m_Zombie" and obj:FindFirstChild("Sword", true)) or
                                    (name == "红眼" and obj:FindFirstChild("E", true) and not obj:FindFirstChild("Sword", true)) or
                                    (name == "大师兄" and obj:FindFirstChild("Axe", true)) or
                                    (name == "燃烧者" and obj:FindFirstChild("Whale Oil Lantern", true))) then
                                    local color = (name == "自爆" and Color3.fromRGB(255,255,0)) or
                                                  (name == "二师兄" and Color3.fromRGB(0,255,100)) or
                                                  (name == "红眼" and Color3.fromRGB(255,0,0)) or
                                                  (name == "大师兄" and Color3.fromRGB(0,100,255)) or
                                                  (name == "燃烧者" and Color3.fromRGB(255,69,0))
                                    createESP(obj, color, name)
                                end
                            end
                        end
                    end
                end)
            else
                for m, d in pairs(espHighlights) do
                    if d.hl then d.hl:Destroy() end
                    if d.gui then d.gui:Destroy() end
                    espHighlights[m] = nil
                end
            end
        end
    })
end

-- ==================== 职业功能选项卡 ====================
local ClassGroup = Tab_Class:AddLeftGroupbox("医疗兵")
ClassGroup:AddToggle({
    Title = "自动请求治疗濒死玩家",
    Default = false,
    Callback = function(state)
        AutoRequestHealEnabled = state
        Notify("医疗兵", state and "自动治疗已开启" or "已关闭", 1)
        if state then
            task.spawn(function()
                while AutoRequestHealEnabled and task.wait(0.25) do
                    for _, player in ipairs(Players:GetPlayers()) do
                        if player ~= LP and player.Character then
                            local humanoid = player.Character:FindFirstChildWhichIsA("Humanoid")
                            if humanoid and humanoid.Health <= humanoid.MaxHealth * (HealThreshold/100) then
                                local char = LP.Character
                                if char and char:FindFirstChild("Medical Supplies") then
                                    local remote = char["Medical Supplies"]:FindFirstChild("RemoteEvent")
                                    if remote then
                                        pcall(function() remote:FireServer("SendRequest", humanoid) end)
                                    end
                                end
                            end
                        end
                    end
                end
            end)
        end
    end
})
ClassGroup:AddSlider({
    Title = "治疗阈值 (%)",
    Default = 25,
    Min = 1,
    Max = 100,
    Callback = function(val) HealThreshold = val end
})

local SapperGroup = Tab_Class:AddRightGroupbox("工兵")
SapperGroup:AddToggle({
    Title = "自动修复建筑",
    Default = false,
    Callback = function(state)
        AutoRepairEnabled = state
        Notify("工兵", state and "自动修复已开启" or "已关闭", 1)
        if state then
            task.spawn(function()
                while AutoRepairEnabled and task.wait(0.1) do
                    local char = LP.Character
                    if not char then continue end
                    local hammer = char:FindFirstChild("Hammer")
                    if not hammer or not hammer:FindFirstChild("RemoteEvent") then continue end
                    local origin = Workspace.CurrentCamera.CFrame.Position
                    local direction = Workspace.CurrentCamera.CFrame.LookVector * 50
                    local params = RaycastParams.new()
                    params.FilterDescendantsInstances = {LP.Character}
                    params.FilterType = Enum.RaycastFilterType.Blacklist
                    local result = Workspace:Raycast(origin, direction, params)
                    if result then
                        local hit = result.Instance
                        local model = hit:FindFirstAncestorOfClass("Model")
                        if model then
                            local buildHealth = model:FindFirstChild("BuildingHealth") or (model.Parent and model.Parent:FindFirstChild("BuildingHealth"))
                            if buildHealth and buildHealth.Value < (buildHealth:GetAttribute("MaxHealth") or 999) then
                                pcall(function() hammer.RemoteEvent:FireServer("Repair", buildHealth) end)
                            end
                        end
                    end
                end
            end)
        end
    end
})

local ChaplainGroup = Tab_Class:AddLeftGroupbox("牧师")
ChaplainGroup:AddToggle({
    Title = "自动祝福感染玩家",
    Default = false,
    Callback = function(state)
        AutoBlessEnabled = state
        Notify("牧师", state and "自动祝福已开启" or "已关闭", 1)
        if state then
            task.spawn(function()
                while AutoBlessEnabled and task.wait(0.25) do
                    for _, player in ipairs(Players:GetPlayers()) do
                        if player ~= LP and player.Character then
                            local infectedVal = nil
                            local wsPlayers = workspace:FindFirstChild("Players")
                            if wsPlayers then
                                local pFolder = wsPlayers:FindFirstChild(player.Name)
                                if pFolder and pFolder:FindFirstChild("UserStates") then
                                    local inf = pFolder.UserStates:FindFirstChild("Infected")
                                    if inf then infectedVal = inf.Value end
                                end
                            end
                            if infectedVal and infectedVal >= BlessThreshold then
                                local char = LP.Character
                                local blessing = char and char:FindFirstChild("Blessing")
                                if blessing and blessing:FindFirstChild("RemoteEvent") then
                                    pcall(function() blessing.RemoteEvent:FireServer("SendRequest", player.Character:FindFirstChildWhichIsA("Humanoid")) end)
                                end
                            end
                        end
                    end
                end
            end)
        end
    end
})
ChaplainGroup:AddSlider({
    Title = "感染阈值",
    Default = 50,
    Min = 0,
    Max = 200,
    Callback = function(val) BlessThreshold = val end
})

-- 自动敲钟/水桶/修桥等
local AutoGroup = Tab_Class:AddRightGroupbox("自动化")
AutoGroup:AddToggle({
    Title = "自动敲钟",
    Default = false,
    Callback = function(state)
        AutoBellActive = state
        Notify("自动敲钟", state and "已开启" or "已关闭", 1)
        if state then
            task.spawn(function()
                while AutoBellActive and task.wait(0.5) do
                    local leipzig = workspace:FindFirstChild("Leipzig")
                    if leipzig and leipzig:FindFirstChild("Modes") then
                        local modes = leipzig.Modes
                        if modes:FindFirstChild("Objective") then
                            local objective = modes.Objective
                            if objective:FindFirstChild("BellInteract") then
                                local bell = objective.BellInteract
                                if bell:FindFirstChild("Interact") then
                                    pcall(function() bell.Interact:FireServer() end)
                                end
                            end
                        end
                    end
                end
            end)
        end
    end
})
AutoGroup:AddToggle({
    Title = "自动水桶灭火",
    Default = false,
    Callback = function(state)
        AutoBucketActive = state
        Notify("自动水桶", state and "已开启" or "已关闭", 1)
        if state then
            task.spawn(function()
                while AutoBucketActive and task.wait(0.5) do
                    local char = LP.Character
                    if char then
                        local bucket = char:FindFirstChild("Water Bucket") or LP.Backpack:FindFirstChild("Water Bucket")
                        if bucket and bucket:FindFirstChild("RemoteEvent") then
                            if bucket.Parent ~= char then bucket.Parent = char; task.wait(0.1) end
                            pcall(function() bucket.RemoteEvent:FireServer("Throw") end)
                            task.wait(0.2)
                            if bucket.Parent == char then bucket.Parent = LP.Backpack end
                        end
                    end
                end
            end)
        end
    end
})
AutoGroup:AddToggle({
    Title = "自动修桥",
    Default = false,
    Callback = function(state)
        AutoRepairBridge = state
        Notify("自动修桥", state and "已开启" or "已关闭", 1)
        if state then
            task.spawn(function()
                while AutoRepairBridge and task.wait(0.5) do
                    local char = LP.Character
                    if char then
                        local bridge = workspace:FindFirstChild("Berezina")
                        if bridge then bridge = bridge:FindFirstChild("Modes") end
                        if bridge then bridge = bridge:FindFirstChild("Holdout") end
                        if bridge then bridge = bridge:FindFirstChild("Bridge") end
                        if bridge then
                            for _, section in ipairs(bridge:GetChildren()) do
                                local posts = section:FindFirstChild("Posts")
                                if posts then
                                    for _, part in ipairs(posts:GetChildren()) do
                                        if part:IsA("BasePart") and part:FindFirstChild("ConstructHealth") then
                                            local hammer = char:FindFirstChild("Hammer") or LP.Backpack:FindFirstChild("Hammer")
                                            if hammer and hammer:FindFirstChild("RemoteEvent") then
                                                pcall(function() hammer.RemoteEvent:FireServer("Repair", part.ConstructHealth) end)
                                            end
                                        end
                                    end
                                end
                                local beam = section:FindFirstChild("Beam")
                                if beam and beam:FindFirstChild("ConstructHealth") then
                                    local hammer = char:FindFirstChild("Hammer") or LP.Backpack:FindFirstChild("Hammer")
                                    if hammer and hammer:FindFirstChild("RemoteEvent") then
                                        pcall(function() hammer.RemoteEvent:FireServer("Repair", beam.ConstructHealth) end)
                                    end
                                end
                            end
                        end
                    end
                end
            end)
        end
    end
})
AutoGroup:AddToggle({
    Title = "自动拿木头",
    Default = false,
    Callback = function(state)
        AutoGetLogs = state
        Notify("自动拿木头", state and "已开启" or "已关闭", 1)
        if state then
            task.spawn(function()
                while AutoGetLogs and task.wait(0.5) do
                    local remote = workspace:FindFirstChild("Berezina")
                    if remote then remote = remote:FindFirstChild("Modes") end
                    if remote then remote = remote:FindFirstChild("Holdout") end
                    if remote then remote = remote:FindFirstChild("Log") end
                    if remote then remote = remote:FindFirstChild("Log") end
                    if remote then remote = remote:FindFirstChild("Interact") end
                    if remote and remote:IsA("RemoteEvent") then pcall(function() remote:FireServer() end) end
                end
            end)
        end
    end
})
AutoGroup:AddToggle({
    Title = "自动放木头",
    Default = false,
    Callback = function(state)
        AutoPlaceLogs = state
        Notify("自动放木头", state and "已开启" or "已关闭", 1)
        if state then
            task.spawn(function()
                while AutoPlaceLogs and task.wait(0.5) do
                    for _, prompt in pairs(workspace:GetDescendants()) do
                        if prompt:IsA("ProximityPrompt") and prompt.Name == "PlaceLogProximityPrompt" then
                            pcall(function() fireproximityprompt(prompt) end)
                        end
                    end
                end
            end)
        end
    end
})

-- ==================== 玩家功能选项卡 ====================
local PlayerGroup = Tab_Player:AddLeftGroupbox("全图高亮 & 移动")
PlayerGroup:AddToggle({
    Title = "全图高亮",
    Default = false,
    Callback = function(state)
        FULL_BRIGHT_ON = state
        if state then
            Lighting.Ambient = Color3.new(1,1,1)
            Lighting.OutdoorAmbient = Color3.new(1,1,1)
            Lighting.Brightness = 2
            Lighting.ClockTime = 14
        else
            Lighting.Ambient = Color3.new(0,0,0)
            Lighting.OutdoorAmbient = Color3.new(0,0,0)
            Lighting.Brightness = 1
            Lighting.ClockTime = 12
        end
        Notify("全图高亮", state and "已开启" or "已关闭", 1)
    end
})
PlayerGroup:AddSlider({
    Title = "移动速度",
    Default = 16,
    Min = 16,
    Max = 45,
    Callback = function(val)
        LOOP_SPEED_VAL = val
        local hum = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.WalkSpeed = val end
        Notify("移动速度", "已设置为 " .. val, 1)
    end
})
PlayerGroup:AddToggle({
    Title = "无减速效果",
    Default = false,
    Callback = function(state)
        NoSlowActive = state
        if state then
            local char = LP.Character
            if char then
                local hum = char:FindFirstChildOfClass("Humanoid")
                if hum then
                    hum:GetPropertyChangedSignal("WalkSpeed"):Connect(function()
                        if hum.WalkSpeed < 16 then hum.WalkSpeed = 16 end
                    end)
                    if hum.WalkSpeed < 16 then hum.WalkSpeed = 16 end
                end
            end
        end
        Notify("无减速", state and "已开启" or "已关闭", 1)
    end
})
PlayerGroup:AddToggle({
    Title = "防断腿",
    Default = false,
    Callback = function(state)
        NoFallDamageEnabled = state
        Notify("防断腿", state and "已开启" or "已关闭", 1)
    end
})
PlayerGroup:AddToggle({
    Title = "自救 (被抓住时自动攻击)",
    Default = false,
    Callback = function(state)
        SelfRescueActive = state
        if state then
            task.spawn(function()
                while SelfRescueActive and task.wait(0.1) do
                    local char = LP.Character
                    if not char then continue end
                    local weapon = nil
                    for _, child in pairs(char:GetChildren()) do
                        if child:GetAttribute("Melee") then weapon = child; break end
                    end
                    if not weapon then
                        for _, child in pairs(LP.Backpack:GetChildren()) do
                            if child:GetAttribute("Melee") then weapon = child; break end
                        end
                    end
                    if weapon then
                        local zombies = workspace:FindFirstChild("Zombies")
                        if zombies then
                            for _, zombie in pairs(zombies:GetChildren()) do
                                if zombie:FindFirstChild("HumanoidRootPart") then
                                    local dist = (char.HumanoidRootPart.Position - zombie.HumanoidRootPart.Position).Magnitude
                                    if dist <= 5 then
                                        if weapon.Parent ~= char then weapon.Parent = char; task.wait(0.1) end
                                        char.HumanoidRootPart.CFrame = CFrame.lookAt(char.HumanoidRootPart.Position, zombie.HumanoidRootPart.Position)
                                        local remote = char:FindFirstChild(weapon.Name) and char[weapon.Name]:FindFirstChild("RemoteEvent")
                                        if remote then
                                            remote:FireServer("Swing", "Side")
                                            remote:FireServer("HitZombie", zombie, zombie.Head.CFrame.Position, true)
                                        end
                                        task.wait(0.1)
                                    end
                                end
                            end
                        end
                    end
                end
            end)
        end
        Notify("自救", state and "已开启" or "已关闭", 1)
    end
})

local PlayerESPGroup = Tab_Player:AddRightGroupbox("玩家透视")
PlayerESPGroup:AddToggle({
    Title = "玩家透视 (Chams)",
    Default = false,
    Callback = function(state)
        PlayerChamsEnabled = state
        Notify("玩家透视", state and "已开启" or "已关闭", 1)
        if state then
            task.spawn(function()
                while PlayerChamsEnabled and task.wait(0.5) do
                    for _, pl in ipairs(Players:GetPlayers()) do
                        if pl ~= LP and pl.Character then
                            local highlight = pl.Character:FindFirstChild("Ziaan_Cham_Highlight")
                            if not highlight then
                                highlight = Instance.new("Highlight")
                                highlight.Name = "Ziaan_Cham_Highlight"
                                highlight.Adornee = pl.Character
                                highlight.FillColor = Color3.fromRGB(0,255,0)
                                highlight.FillTransparency = 0.45
                                highlight.Parent = pl.Character
                            end
                        end
                    end
                end
            end)
        else
            for _, pl in ipairs(Players:GetPlayers()) do
                if pl.Character then
                    local hl = pl.Character:FindFirstChild("Ziaan_Cham_Highlight")
                    if hl then hl:Destroy() end
                end
            end
        end
    end
})
PlayerESPGroup:AddToggle({
    Title = "低生命值玩家通知",
    Default = false,
    Callback = function(state)
        NotifyLowHealth = state
        if state then
            task.spawn(function()
                while NotifyLowHealth and task.wait(1) do
                    for _, pl in ipairs(Players:GetPlayers()) do
                        if pl ~= LP and pl.Character then
                            local hum = pl.Character:FindFirstChildOfClass("Humanoid")
                            if hum and hum.Health <= hum.MaxHealth * 0.25 then
                                Notify("低生命值", pl.Name .. " 生命值低于25%", 2)
                            end
                        end
                    end
                end
            end)
        end
    end
})

-- 添加获取物品按钮（原第二个文件中的Voivode, Iron Stake）
local GetGroup = Tab_Main:AddRightGroupbox("获取物品")
GetGroup:AddButton({
    Title = "获取 Voivode",
    Callback = function() purchaseItem("Voivode"); Notify("获取物品", "已尝试获取 Voivode", 1) end
})
GetGroup:AddButton({
    Title = "获取 Iron Stake",
    Callback = function() purchaseItem("Iron Stake"); Notify("获取物品", "已尝试获取 Iron Stake", 1) end
})

print("SDGT | 水调歌头 已加载，使用 WindUI 界面")
